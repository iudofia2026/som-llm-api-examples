# Copy to .env locally if desired. Never commit real keys.
SOM_LLM_KEY=sk-som-...
SOM_LLM_BASE_URL=https://api.som.chat/v1
# Normally leave SOM_LLM_MODEL unset so examples use /v1/models.
# SOM_LLM_MODEL=
