name: examples

# MANUAL ONLY (2026-08-23). Auto triggers removed — iudofia2026 Actions quota.
on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: scripts/check-examples.sh
