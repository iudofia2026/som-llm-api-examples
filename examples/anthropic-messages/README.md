# Anthropic-compatible Messages

Basic Anthropic Messages request using `httpx` against `https://api.som.chat/v1/messages`.

Offline validation:

```sh
python3 example.py --self-test
```

Live run:

```sh
SOM_LLM_KEY=sk-som-... ./example.py --live
```
