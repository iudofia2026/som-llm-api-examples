# OpenAI-compatible chat

Basic chat completion call using the standard OpenAI Python client.

Offline validation:

```sh
python3 example.py --self-test
```

Live run:

```sh
SOM_LLM_KEY=sk-som-... ./example.py --live
```
